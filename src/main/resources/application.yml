cloud:
  aws:
    credentials:
      access-key: AWSACCESSKEY
      secret-key: AWSSECRETKEY
    region:
      static: ap-south-1
    stack:
      auto: false

application:
  bucket:
    name: modestcars-mum-img-storage

server:
  port: 9090